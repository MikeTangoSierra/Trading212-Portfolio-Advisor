# Trading212-Portfolio-Advisor
A Python based application to work alongside the 'Trading212-Portfolio-Poller' Python application by querying the MongoDB database that it writes to, transforming the data and feeding it into an LLM to get advice about the data.
